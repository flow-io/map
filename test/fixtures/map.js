'use strict';

/**
* FUNCTION: map( chunk, idx )
*	Mocks a map function.
*
* @param {*} chunk - streamed value
* @param {Number} idx - chunk index
* @returns {Number} mock return value
*/
function map() {
	return 5;
} // end FUNCTION map()


// EXPORTS //

module.exports = map;
